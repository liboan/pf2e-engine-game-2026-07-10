# Content W4 execution and W5 continuation working model

W4 is authorized from published W3 checkpoint `33ce54fc07a82af184fff09c4c6883c8b436b04b` (accepted implementation `832d44b6816cd42d2c9cc9d3cfc2753d906774d1`). This file replaces earlier milestone routing. W1–W3 remain accepted; historical archives are evidence, not current instructions. No extra approval round is required for routine W4 roster choices after a brief feasibility pass. W5 is also authorized, strictly after W4 full assembled acceptance and established publication. W4 owners/reviewers remain through all normal repairs.

## Authority, team and checkouts

The existing coordinator `01a0bc89-010f-7f33-8ea1-e359e6ed6a93` remains Astra/high and owns scope, guidance, recovery, assembled acceptance and publishing. It reads repository metadata and compact evidence, not implementation or rules-source content. Actual implementation owners inspect code, resolve ordinary contracts and integrate.

All W4 implementation lanes use **fresh full Luna/high tasks**; all independent reviewers use **fresh full Sol/high tasks**. Each starts in its own fresh managed worktree with accepted W3 code plus current committed guidance. Current W4 tasks use title prefix **PF2e Content W4 — **; fresh W5 tasks will use **PF2e Content W5 — **. Fresh implementers and reviewers per substantial milestone are the default; the same owners and reviewers remain through this milestone and ordinary repairs. No automatic model/owner replacement after a failing test or finding. A specific substantial unresolved design/rules question may receive bounded Astra/high decision work. No silent model substitutions or native-subagent dependency.

One coherent family owner owns legal grants, runtime, terminal, persistence and tests. Three independent lanes: offensive martial class feats; defensive/mobility/skill-linked class feats; caster/support class feats and similarly inexpensive spell/item reuse. The caster/support owner is also integration lead in its own checkout. Two reviewers cover offensive+defensive lanes and caster/support+final assembled review respectively. Exact IDs, roles and current bases live in [ACTIVE](../work-log/ACTIVE.md). Prior W1–W3 workers are closed and not W4 assignees.

Do not reuse another task's mutable checkout, create nested worktrees or reset away work. Branches may be created within the assigned fresh checkout. Reviewers inspect explicitly delivered commits in their own checkout; preserve their review tests. Integration lead incorporates accepted commits/tests deliberately and coordinates semantic conflicts with owners. Coordinator merges only the independently accepted assembled result. Leave old refs/worktrees alone.

## Scope and brief feasibility

Prioritize materially broader genuinely new playable content, especially level-1 class feats across accepted classes. Planning ambition is roughly a dozen distinct new feats across the lanes, plus spells/items where equally inexpensive reuse is useful. This is a scope target, not a count quota: alternate class configurations of existing names, definitions, placeholders and unselected candidates do not count as new content. W4 must materially exceed W3's five distinct feats and two items while preserving correctness. Do not stop after one small option; if the feasible roster cannot support that ambition, send the smallest substantive scope decision rather than pad delivery.

Each owner briefly checks a related candidate set against the accepted catalog and applicable sources, selects its actual roster, records legal grants/material differences/reusable helpers/deferred prerequisites in a scoped note, then immediately implements. No approval per feat and no exhaustive inventory. Review completed runnable groups. Lane definitions and startup pointers are in [W4 handoff](07-content-w4-handoff.md). Source-check candidate names before commitment. Content may be chosen within existing L1/L2 menus; do not raise representative levels or admit unsupported behavior silently.

Reuse established mechanics and proactively simplify touched branching. Split oversized mixed-responsibility modules cohesively, extract shared behavior with real adopters and delete replaced paths before or during extension. Keep refactors proportional to playable delivery. No infrastructure-only wave or speculative universal framework. Each shared abstraction has one named owner and a concrete API/semantic contract. Coordinate dependency changes directly with affected peers; independent useful work stays parallel. Avoid simultaneous wholesale rewrites of encounter/persistence/terminal; owners extract narrowly scoped family seams, integration lead resolves overlap deliberately.

## Checks and independent acceptance

Before changing production semantics or a failing assertion, verify the applicable source and actual fixture. Check legal grants, action/trait/cost timing, hand/weapon/target requirements, MAP, reactions/choices, typed bonuses, immunity and expiry. Preserve printed differences and source citations in scoped notes/tests. Same owner repairs normal review findings, directly with its assigned reviewer.

Use focused bounded checks frequently and internally. One synchronous pytest process per task; no xdist, watches or background tests. Only integration lead runs the broad assembled checkpoint while active; review groups use bounded appropriate checks and coordinate any necessary full-run overlap concretely, without a global focused-test lock.

Verified inherited environment: `/Library/Frameworks/Python.framework/Versions/3.11/bin/python3`, Python3.11.1, pytest8.4.2, `PYTHONPATH=src`, `/opt/homebrew/bin/timeout`. Verify once in each fresh checkout; do not assume a copied venv. Focused command: `PYTHONPATH=src /opt/homebrew/bin/timeout 30s /Library/Frameworks/Python.framework/Versions/3.11/bin/python3 -m pytest -q <selection>`. Final canonical command: `PYTHONPATH=src /Library/Frameworks/Python.framework/Versions/3.11/bin/python3 tools/integration_checkpoint.py`. Inspect runner options rather than invent flags; bound terminal input/time/output with existing helpers.

Acceptance requires legal/source-grounded complete runnable families, real continuous public play to completion, adverse cases, literal save/load, actual terminal choices and bounded performance. Lane acceptance is **not** assembled acceptance. After all lanes are integrated, the assigned Sol assembled reviewer must independently exercise cross-family interactions at an explicit assembled head and return accepted tests/findings to integration lead. Lead incorporates those tests and all repairs, then runs final full suite and canonical compile/test/diff/catalog/peak-memory gate. Only then send coordinator one assembled acceptance handoff with actual delivered names versus deferred candidates and exact verified commit IDs.

At handoff/idle/integration report task-owned test/probe status. Gracefully stop only identity-verified unused task-owned descendants; never kill by name or claim host cleanup when process inspection is unavailable. Unavailable OS inspection is a stated limit.

## Compact handoffs and message discipline

Assignments and handoffs point to this working model, ACTIVE and the scoped W4 handoff. Read current guidance and relevant code/source sections; do not reconstruct old transcripts or load whole archives. Owner delivery normally 250–400 words: exact owned files/symbols; shared helpers and one analogous test; actual cwd/interpreter/PYTHONPATH/commands; last successful public probe or explicitly none; applicable rules references; current API contracts/binding decisions; unfinished dependencies; exact commit/tests and process status. Owner verifies symbols locally before claiming an API. Reviewer returns consolidated findings or accepted commits/tests to integration lead.

Full-task `send_message_to_thread` exchanges are authorized. Send only when the recipient's next action changes. Owners ↔ assigned reviewer handle ordinary delivery/repairs. Reviewers alone send accepted lane evidence to integration lead; owners do not duplicate acceptance. No coordinator CCs, acknowledgements, receipt loops, repeated review requests, routine status requests or per-test narration. Notify affected peers of actionable dependency changes. Coordinator contact only for genuine scope/priority/shared-contract/unresolved blockers or final assembled acceptance.

**END promptly whenever only autonomous peer work remains.** Reviewers may perform useful bounded preparation then end until a substantive runnable group arrives. Owners report a dependency once and end unless independent work remains. No waits/sleep/status/transcript/commentary loops. The coordinator uses only its existing five-minute execution heartbeat as fallback; no worker polling schedules. On each wake inspect compact state once, act only on meaningful decisions or assembled acceptance, otherwise end silently. Continue this same heartbeat across W4 acceptance/publication into authorized W5; pause at W5 closure or explicit user pause. Keep older coordinators/heartbeats paused.

## Boundaries, recovery and publication

Preserve accepted S1–S3, eleven Barbarian builds and all sixteen selected L1/L2 representatives within finite spell/domain/formula/nested menus. Ranger remains combat-only, Monk horizontal-only; above-level test equipment stays labeled. Vertical terrain/High Jump and positive damage to stable unconscious PCs at zero remain unsupported. Pending paired mixed-resistance and pre-first-turn Surprise Attack questions are not silently resolved. Existing GM conventions in the relevant class-plan sections bind. No generic poison/minion/universal rules framework.

Coordinator alone edits central plans/work logs at meaningful milestones. Owners maintain scoped source/implementation notes and tests. ACTIVE contains identities/model/cwd/base/scope/peers/dependencies/checkpoint/next action. Cost monitoring remains with audit `01a0a8e5-7dec-7030-8679-120b66f70e44`; reuse existing `tools/log_agent_usage.py` only if accounting is needed, no new metrics system or guessed unavailable usage.

At closure publish accepted W4 code/tests/guidance and integration fixes using the established non-force codex/ origin branch workflow. Inspect current PR/remote state; no main merge or unrelated overwrite. Preserve accepted W2/W3 and verify accepted W4 ancestry, exact local/remote delivery heads and clean checkout. A new PR is optional. Finish each wave’s authorized publication in its closure turn. Leave a concise accepted-checkpoint handoff for the next fresh team: delivered versus deferred content, relevant exact symbols/helpers/tests, verified environment/commands, executable evidence, binding contracts and remaining boundaries. After W4 acceptance/publication, proceed to the authorized W5 process below. Pause the existing heartbeat at W5 closure or explicit user pause; work beyond W5 requires separate authorization.


## Authorized W5 transition and stronger upfront selection

Do not begin W5 implementation or reset W4 workers before W4 is fully accepted through its independent assembled review, final validation and publication. Preserve current owners through ordinary W4 repairs. The coordinator remains the same Astra/high task.

After publishing W4, create **new full tasks and new isolated worktrees for every W5 implementation and review lane**, all based on the accepted W4 checkpoint plus current guidance. Luna/high owns all implementation; Sol/high owns all reviews. No W4 implementer or reviewer conversation is carried into W5; no forks of accumulated transcripts. Fresh contexts are expressly required, including for workers that compacted in W4. Keep W5 owners/reviewers through its normal repairs.

Astra sets high-value priorities and coherent family boundaries from accepted/deferred evidence. Fresh Sol reviewers then perform a brief bounded source-and-code feasibility pass for those families, recommending named feat/content batches, actual reusable mechanics and exact symbols/helpers, necessary extensions, legal grant/level constraints and key public/save/terminal/adverse acceptance cases. Check what is already playable so variants of existing names cannot pad breadth. Return a compact actionable recommendation, not exhaustive inventories or large work packets. This is an authorized planning decision handoff; it is not a reason for acknowledgement or status traffic.

The coordinator uses those recommendations to finalize concrete parallel lane assignments. Fresh Luna owners implement the resulting roster and flag material scope discoveries to the affected peer/coordinator only when a decision is required. Routine choices need no additional user approval. Sol recommendations are hypotheses to independently verify during review, not substitutes for source-grounded execution and final assembled interaction checks.

W5 should widen actual playable coverage materially again, emphasizing additional level-1 class feats and coherent related adopters of existing mechanics, with worthwhile inexpensive spell/item reuse. Keep proportional splitting/simplification and playable outcomes; no count padding or infrastructure-only wave. Existing supported boundaries remain binding.

Before W5 dispatch, replace W4-specific active assignments in recovery with the exact fresh W5 identities/checkouts/baseline and write the compact accepted-W4 handoff. Every new assignment points to this model, ACTIVE and that handoff. Provide relevant exact symbols/helpers/analogous tests, verified commands/environment/cwd, source/evidence pointers, decisions and supported limits; no old transcript reconstruction. The same strict direct owner/reviewer/integration routing, end-turn discipline, final independent assembled review, canonical gate and verified publishing workflow applies. Monitoring remains in the separate audit task.
