# W4 compact startup handoff

Read [working model](04-delivery-and-checks.md) and [ACTIVE](../work-log/ACTIVE.md). Accepted code checkpoint: `832d44b6816cd42d2c9cc9d3cfc2753d906774d1`; published W3+guidance: `33ce54fc07a82af184fff09c4c6883c8b436b04b`. Fresh W4 checkouts inherit identical code plus new guidance. Baseline gate 1,457 tests, accepted105/83, staged24/12, save18. This is inherited evidence, not a W4 test claim. No W4 public probe yet.

## Lane boundaries and feasibility

1. **Offensive feats:** substantial related L1 class feats using Strike/attack/continuation and target effects across Fighter/Rogue/Ranger/Barbarian/Monk where legal. Candidate directions include exacting attacks, paired feints, agile multiattacks and hunt-linked shots; check existing coverage first. Avoid re-counting W3 Snagging Strike/Combat Grab/Brutish Shove or W1 Intimidating Strike. Own new offensive continuation helpers and semantics. Relevant files: `src/pf2e/fighter.py`, `l2_martial_content.py`, `encounter.py`, `persistence.py`, `terminal.py` and selected class modules. Analogous tests: `tests/test_w3_strike_riders.py`, `tests/test_w3_assembled_interactions_review.py`.
2. **Defense/mobility/skill-linked feats:** substantial related L1 class feats whose core behavior reuses existing reactions, movement, feints, demoralize or typed defensive effects across legal martial classes. Coordinate offensive activity boundaries directly with lane1. Avoid re-counting existing Nimble Dodge, Dueling Parry or Crane Stance. Own new defensive/mobility helper semantics; preserve horizontal movement scope. Relevant files: `src/pf2e/martial_defense.py`, `skill_actions.py`, selected class modules and narrow encounter/persistence/terminal seams. Analogous tests: `tests/test_w2_martial_defense.py`, existing selected-class skill/reaction tests found with scoped rg.
3. **Caster/support and integration:** substantial new L1 class-feat behavior across legal casters with finite menus, reusing existing spell attacks/saves/healing/damage/conditions/preparation and reactions. Check viable divine healing/harming, caster damage/support and focus/cantrip enhancement families; inexpensive related spells/items are allowed where they produce actual new behavior. Widen/Reach/Cantrip Expansion names already exist and are not new-count credit. Own caster/support helpers and wave integration. Relevant files: `src/pf2e/family_casting.py`, `spellshape.py`, `w3_caster_content.py`, selected caster modules and narrow runtime/persistence/terminal seams; item files only if chosen. Analogous tests: `tests/test_w3_caster_spellshape.py`, `tests/test_w3_caster_independent_review.py`, `tests/test_w2_suppression_spells.py`.

These are feasibility directions, not asserted legal/implemented feats. Each owner selects a source-backed actual roster after a brief local pass, aiming a substantial batch (roughly four or more distinct new feats where feasible), and proceeds. Across lanes, aim materially beyond W3's five names/two items without count padding. Additional related options are welcome when cheap actual reuse supports them. If a lane cannot deliver a substantial roster, escalate that concrete scope constraint early. Avoid exhaustive planning and speculative prerequisite frameworks.

## API and source recovery

The coordinator has not inspected implementation or verified symbol-level APIs. Owners must identify exact callable symbols and current contracts with narrow local `rg`/reads before editing and record them in their scoped feasibility/delivery note; do not infer API names from prose. Preserve existing public calls and literal save validation. Shared seams already include Strike continuations, timed conditions, spell saves/basic damage, preparation, spellshape marker consumption and bomb facts/effect provenance. One owner per new abstraction; communicate actionable signature/semantic dependencies to affected peers and integration lead.

Use the applicable Paizo rules and Archives of Nethys entries linked in relevant existing tests/source packets; browse specific current feat/spell/item pages to verify the selected roster. Do not load whole source inventories or historical transcripts. Preserve pending decisions and documented GM conventions from relevant sections of `06-class-and-content-expansion.md`.

## Environment and handoff

Inherited interpreter `/Library/Frameworks/Python.framework/Versions/3.11/bin/python3` (3.11.1), pytest8.4.2; verify in your assigned cwd. `PYTHONPATH=src /opt/homebrew/bin/timeout 30s /Library/Frameworks/Python.framework/Versions/3.11/bin/python3 -m pytest -q <focused selection>`. Integration uses `tools/integration_checkpoint.py`; one synchronous pytest per worker, one broad integration. No W4 public probe exists at dispatch. The accepted W3 public reference is the saved Dread/Grab/Widen encounter and three Fighter terminal actions in `test_w3_assembled_interactions_review.py`.

Ready handoffs must supply actual owned symbols/files, reused helpers, analogous test, verified cwd/environment/commands, last successful public probe, applicable rules URLs, API contracts, binding decisions, unfinished dependencies, exact commits/checks and task-owned process status. Reviewer sends accepted lanes to integration lead. Caster/support reviewer also performs independent final assembled interaction review before lead's final canonical gate. No missing-gate substitution by full-suite counts alone.
