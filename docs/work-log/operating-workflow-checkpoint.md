# Historical operating workflow evidence

This record describes earlier execution. Current instructions are in [the Content W1 working model](../plan/04-delivery-and-checks.md); historical native-only communication and routing here are superseded.

# Operating workflow checkpoint

## Outcome

The engine work now resumes from one short [current-state page](ACTIVE.md). Past evidence remains archived and linked, rather than being loaded again to reconstruct ownership. AGENTS, README, STATUS and the delivery plan point to this working model.

Worker reports arrive at a decision, a shared interface change, the first executable checkpoint or completion. They use internal collaboration or normal final reports; the supervisor gives the user a consolidated account. Independent rules review, actual Python play, focused tests, saved decisions and bounded memory checks remain required.

One collector command handles assignment, completion and usage summaries. It reads per-request usage metadata, including compaction requests when present, instead of relying solely on older running totals. Fresh runs begin at local session metadata; reused workers receive a boundary before their follow-up. Original legacy evidence is retained. Missing raw records are unavailable, not zero or invented usage. The [delivery plan](../plan/04-delivery-and-checks.md) contains the commands.

## Verification

A separate Luna worker reproduced and repaired an inherited-parent identity edge: a record with a parent `session_id` and no `thread_id` could previously be charged to the child. The regression and remaining focused collector tests pass: **7 tests in 0.443 seconds**. The collector self-test passes.

The independent real-session check found 30 local request records with the observed Luna/xhigh configuration. Fresh accounting matched that session boundary. A reused boundary with no later requests did not charge the lifetime usage again. This real sample contained no compaction requests; compaction handling was tested with the focused fixtures, not demonstrated by that sample.

A later real spell-targeting run also reports one compaction request: `dim-core-spell-gate-2026-09-16` has 88 deduplicated requests, input 11,091,484, cached input 10,863,616 and output 52,321, with observed Luna/xhigh. Cached input is included in input, not an additional total. This is one reused assignment's measured usage, not the worker's lifetime total; its dispatch boundary was recorded before the follow-up. The ledger retains the raw accounting source and availability status.

A legacy session without raw request records retained its old token-count evidence in history and reported current raw usage unavailable. Older runs have not all been re-audited. The newly completed collector, documentation and identity-test rows were refreshed after the identity-filter repair.

Documentation validation checked **77 local links and one fragment**, with none broken. All reported checks were synchronous and exited; no background processes were launched. This is not a host-wide process census or a claim that desktop-owned MCP servers stopped.

## Continued use of this model

Subsequent Runic Weapon, Light, targeting, terminal and independent-play work uses the same collector. The updated [delivery guidance](../plan/04-delivery-and-checks.md) now keeps each bounded playable outcome with one Luna owner through runtime, saves, terminal, tests and ordinary review repairs. Only a genuinely new assignment creates a reused boundary; internal checkpoints and test waits do not close runs. Earlier records retain their accurate observed boundaries rather than being rewritten. Current ownership and executable evidence belong in ACTIVE, not this historical checkpoint.

At the Light checkpoint, a dedicated process worker obtained one scoped escalated inventory after ordinary `ps` was denied. It found no descendants attributable to the completed engine workers. Node/Playwright processes belonged to shared ChatGPT.app MCP services; the sole separate Python candidate belonged to an unrelated localclaw benchmark, verified by its working directory. No termination was warranted. Earlier sandbox-denied checks remain honestly recorded as unavailable; the later successful audit does not rewrite their evidence. Continue to use command, parent, start time and working directory before stopping anything, and never kill a shared service by name.

## Archived coordination evidence from ACTIVE — 2026-09-17

Moved from the recovery entry to keep current work easy to find. This is historical evidence, not a new queue or workflow.

## Working model and messaging

The user explicitly chose **Terra/high for all implementers**, replacing Luna/xhigh and the briefly proposed Terra/xhigh setting. Sol/high remains for independent play/review; Astra/high remains for design/investigation. The [owning guidance](../plan/04-delivery-and-checks.md) and delegated AGENTS edit reflect this. Native Terra-to-root and Terra-to-Astra messages were actually delivered; implementation/review messaging also worked during this slice.

Fresh Luna did not solve the earlier messaging limitation. Its model catalog says agent protocol v1, while sampled worker sessions use v2; Sol/Astra/Terra catalog entries are v2 and their direct messaging works. Identical runtime/features and no discovered legacy route make a model/protocol tool-exposure mismatch the leading explanation, but the exact runtime gate is unproven. No cache patch or app restart was attempted. RCA evidence: `/Users/andrewlee/.codex/models_cache.json`, logs rows 1221478155/1221478326 in `logs_2.sqlite`, and `state_5.sqlite` dynamic-tool metadata. Diagnostic runs and exact results are closed in the ledger.

Use native collaboration directly outside `functions.exec`; `ALL_TOOLS` searches do not establish native absence. `send_message` queues information and does not wake idle/completed peers. Owners and reviewers use `followup_task` for preauthorized review activation, within-scope repair requests and dependency responses requiring action, including when a peer may have ended its turn. Peers may call it; a running recipient receives the follow-up without interruption. Submission alone does not prove handling. Preserve scope authority and existing accounting for synchronization/ordinary repairs; only genuinely new assignments require new boundaries. Root handles actionable events during scheduled wakes and ends when no immediate decision remains; no routine wait loop, status pings or acknowledgements. A user-requested one-time synchronization check may collect receipts, after which normal cadence resumes. Preserve source-grounded assertions, bounded probes and same-owner repairs. No new packets, acceptance tables or approval rounds.

The approved cadence also permits one genuinely independent second implementation lane and early Sol consultation on a named source/test uncertainty likely to cause substantial rework. Reuse existing research; require useful independence from active edits. Substantial unresolved design/rules remain Astra work. Neither available capacity nor this procedure creates mandatory extra review/research, and pending user decisions remain pending. The owning [delivery guidance](../plan/04-delivery-and-checks.md) integrates these changes.

**One-time native wakeup synchronization completed.** Root used `followup_task` for all eight retained workers, including the three historical workers outside the initial live listing; no replacements were spawned or productive turns interrupted. Both current reviewers returned actual receipts. Ranger initially acknowledged and ended prematurely; a targeted follow-up then produced the actual bounded 18-test baseline and an explicit report of no tool/runtime/policy blocker. Monk confirmed receipt, activated its reserved reviewer, and responded to the reviewer's native follow-up with the Shield Block repair; the reviewer verified it. Thus the current implementation/review lanes demonstrated responses and actionable handling, rather than only successful submissions. Historical workers reported completed assignments with no pending handoff; no new work was invented. No synchronization accounting assignments were created. The branch worker's read-only audit found no Python/pytest/Playwright/Chrome process to stop. Return to normal five-minute coordination and event-driven worker reports; this was not a recurring broadcast or acknowledgement policy.

## Execution heartbeat and first-cycle verification

Created **`pf2e-content-expansion-execution`**, active, attached to this root task **`01a0a4ea-653e-7fe0-b902-044bc77b041f`**, with a five-minute interval. The inventory found no matching execution heartbeat before creation. Separate audit automation `engine-efficiency-check-1-hour` and the paused old-project orchestration heartbeat were preserved. This is execution coordination, not an audit monitor.

The packaged app scheduler was inspected by Astra: active/waiting target turns are deferred rather than overlapped; an in-flight guard excludes concurrent runs of the same automation. Due time is the later of last-run or relevant thread-update time plus five minutes, with scheduler latency. Whether idle-worker messages extend that cooldown remains empirically untested. Source evidence is in `/Applications/ChatGPT.app/Contents/Resources/app.asar`, `.vite/build/main-DaMR-wdT.js`, functions `ds/bs/Ko/rs/Wo/ms/Ss`. No productive worker was interrupted for this check.

**Scheduled resumption and the retained-worker follow-up are verified.** Automation `pf2e-content-expansion-execution` delivered its first actual heartbeat into this same root task at **2026-09-17 00:11:46.949 UTC**, after the preceding parent final. A later wake arrived at **00:22:47.047 UTC**. The visible heartbeat envelopes establish scheduled resumption; the lower-level `turnTrigger` field was not separately inspected. Setup had been recorded at 00:04:13 UTC before parent completion.

A single compact worker snapshot found retained session `01a0abf0-89f7-7971-acd2-6bafc3e7e2f8` still active in the same Skill Stratagem turn `01a0acab-a700-75a3-9909-81f6a844f1e5`, with a completed command marker and a source-grounded test-preparation report. No worker was restarted or reassigned. Exact command timestamps during the closed-parent interval were not captured, so the evidence is retained activity and the latest report, not a fabricated execution timeline.

Root delivered a useful non-disruptive native follow-up: clarify the reported 'through next turn' wording against the existing 'until next turn' expiry contract in source-grounded tests, continue independently, and mention receipt in the next substantive executable/completion report. The owner explicitly confirmed receipt in its substantive report, tested expiry at the start of the next turn, and completed the retained turn at **00:17:44 UTC**, while the parent turn was closed. This establishes the scheduled follow-up round trip and continued worker completion. No separate acknowledgement or status request was made. Active-turn overlap prevention is source-verified; no artificial overlapping tick was forced. Whether idle-worker messages extend the cooldown remains untested.
On every later wake: inspect once, resolve real decisions/dependencies, accept evidence and dispatch ready authorized work. Leave unchanged state alone and end quietly if nothing needs action. Pause the heartbeat on user pause or actual completion of the full agreed milestone, not on a single slice.

Closed setup research `execution-heartbeat-research-2026-09-16`: Astra/high,807,663input /706,944cached /2,637output,9requests,0compactions. Cached input is part of input; dollar cost unavailable. The real scheduled cycle is verified. Its one-time verification is complete; later wakes follow normal execution coordination.
