# Generalization phase evidence and implementation handoff

September 18, 2026: user authorized this phase before remaining L1/L2. Preserve the designated dirty engine worktree and paused content milestone. Current ownership is in [ACTIVE](ACTIVE.md).

## Initial assessment and sequence

Fresh Astra/high read-only assessment (`generalization-assessment-2026-09-18`) confirms transactions, modifiers/checks, damage/health, casting resources, condition deadlines and subordinate Strikes already share meaningful execution. The prior audit is a set of leads, not a quota or proven failures. Coordinator chooses shared spell-save/provenance first, finite preparation consistency second, admitted bomb-fact consumption third; recipient progression is conditional on substantial remaining duplication after the save seam settles. Serialize shared production edits rather than manufacture parallel work.

### Finite preparation consistency — next group

Actual adopters: Wizard, Druid, Witch. Repeated policy selection: `family_casting._prepared_casting_snapshots`, `Encounter.daily_prepare` (~2615), substitution menu (~1602), persistence prepared-slot restore (~1110) and reconstructed-actor validation (~2668), terminal `_choose_daily_preparations` (~1074). Existing helpers are `wizard.prepared_slot_rejection`, and Druid/Witch `preparation_choices` / `prepared_slot_rejection`.

A finite helper exposing `preparation_choices(actor, definition, slot)` and `prepared_slot_rejection(actor, definition, slot, spell_id)` can centralize routing and fixed-slot fallback. Keep Wizard owned-book/curriculum rules and class menus explicit. Preserve menu ordering, duplicates, source/rank/slot shape/spent state. No new schema or content.

Static correctness lead, not yet confirmed: fixed preparers fall through `daily_prepare` to Wizard validation, which returns None without a spellbook; changed Warpriest slots may be accepted at preparation but rejected in casting/load. Owner must establish a public failing regression before calling it a defect. Persistence validates Druid/Witch with `actor=None` before reconstruction, whereas Wizard needs owned inventory: preserve validation phases or move membership validation to after reconstruction.

Test pointers: `tests/test_storm_druid.py::test_storm_druid_terminal_preparation_menu_allows_changed_duplicate_rank_one_choices`, `test_storm_druid_wins_refocuses_changes_daily_choice_and_casts_in_next_scene`; `tests/test_wizard_substitution.py::_finished_wizard_game` and book/curriculum restrictions; `tests/test_witch_flamekeeper.py::test_healthy_witch_victory_refocuses_reprepares_saves_and_casts_next_scene`. Add cross-path fixed-preparation regression. Preparation overlaps spell ownership in family_casting/persistence; wait for handoff.

### Consume admitted bomb facts — third group

`alchemy_content.BombFacts` already contains damage type, splash, hit rider, deadline and attack facts. `Encounter._quick_bomber`, `_resolve_strike_result` (~5008/5044), `_bomb_damage_with_primary_splash`, `_apply_bomber_miss_primary_splash`, `_apply_bomber_bomb_effects` (~6316) and options (~3584) repeat two-ID switches, splash 1 and lightning/otherwise-cold assumptions.

Use finite admitted lookup through selected attack item definition and `AttackDefinition.item_id`; consume existing facts. Keep the explicit two-bomb admission list `alchemist_content.BOMBER_FIELD_FORMULA_IDS`: other catalog bombs are reference-only. Translate only two supported riders; no effect interpreter. Protect combined initial/primary-splash defenses, undoubled critical splash, miss consumption, distinct deadlines, per-throw choice, infusion provenance, and unsupported miss-splash defense pauses.

Tests: `tests/test_bomber_alchemist.py::test_quick_bomber_draws_selected_bomb_saves_attack_choice_and_consumes_it`, `test_quick_alchemy_created_bomb_round_trips_with_creator_provenance`; `tests/test_bomber_alchemist_review.py` coverage around165/181/213/248/296. Encounter-file contention makes sequencing preferable.

### Conditional recipient progression and stopping

After shared save API settles, evaluate `_resolve_breathe_fire` / `_continue_breathe_fire` and `_resolve_electric_arc` / `_continue_electric_arc` for one roll, recipient index, save, damage and continuation. Begin with two unlike adopters; preserve selection, eligibility, damage profile/riders. Do not store callbacks or replace continuation schema. Caustic/Gale have additional rider-specific progress. Tests in `test_wizard_direct_spells.py`, `test_battle_wizard_combat.py`, `test_storm_druid.py` (saved PC save ownership). Preserve per-recipient Guidance consumption and Hero dice facts.

Paired policies have only two adopters and already share execution; defer absent demonstrated drift. Local next-actor walk extraction is optional, preserving different end-turn and persistent-save prerequisites. Reject poison framework for one admitted poison, generic minions, universal spell profiles, and flattening unique Oracle/Blood Magic/composition rules.

Stop when the three main groups have actual shared adoption, deleted replaced paths, unlike-adopter public play/save evidence, independent review and successful integration. Pursue recipient progression only for meaningful remaining transition duplication. Do not extend this phase into new saved-state concepts, generic callback routing, unsupported content, unresolved rules or merely relocated branches. Report remaining opportunities and diminishing returns.

## Established baseline and validation

Terra baseline: `PYTHONPATH=src .venv/bin/python tools/integration_checkpoint.py`, Python3.11.1 and package imports verified in target worktree. 1,211 passed/5.98s; runner6.373s; peak child81,952,768bytes; compile/diff clean; admitted60/46, staged25/12, save17. Latest broad baseline now includes final Bard corrections.

Independent Sol Bard gap closure: `PYTHONPATH=src .venv/bin/python -m pytest -q tests/test_bard_courageous_anthem.py::test_staged_maestro_sheet_has_legal_level_one_spellcasting_and_declared_limits tests/test_bard_lingering_composition_review.py::test_selected_maestro_sheet_grants_are_legal_and_literal`: **2 passed/0.10s**. Heritage Versatile Human present; no duplicate feat. No files edited. Reviewer initially had a denied process inventory; root obtained a scoped escalated inventory at23:21:53UTC, finding only its own ps/filter commands and no matching retained engine test/probe process. No signal warranted. This closes the limited process-verification gap without claiming host-wide cleanup.

Documentation owner changed only AGENTS/README/STATUS; diff and touched relative-link checks passed. Root changed plan02/04/06 and compacted ACTIVE while preserving the prior recovery in an archive. Actual metadata verifies coordinator Astra/high, owner Terra/high and reviewer Sol/high.

## Closed setup usage

Assessment: actual Astra/high,707,504input /626,560cached /4,501output,12requests,0compactions. Documentation: actual Terra/high,211,160input /190,208cached /4,356output,5requests,0compactions. Cached input is a subset; dollars unavailable. Both assignments closed; no code changes or retained jobs. Spell implementation/review remain open through ordinary fixes. No generalized group is accepted yet.

## First scheduled execution checkpoint — 23:28 UTC

Actual heartbeat envelope observed at 2026-09-18T23:28:24.882Z after the parent final. Retained Terra owner reports public reproduction and repair of prepared-Fear provenance and Tempest/Vitality Guidance gaps; common `_roll_spell_save` owns Guidance, save/modifier calculation and target Hero prompt, with `_resolve_spell_save_result` as the resume map and specialized riders retained. Persistence covers committed source kind/slot across ten admitted save pauses and public-load revalidation. Focused86 passed/0.71s; no final acceptance yet. Native owner activation of reserved Sol succeeded and both workers are running. Coordinator sent a useful same-assignment final-evidence follow-up; response/handling is not yet claimed. No implementation reassignment, new accounting boundary or repeated broad test.

## Spell-save group final checkpoint — September18 23:46 UTC

Terra implemented `_roll_spell_save` (encounter around8572) and `_resolve_spell_save_result` (8896), removing duplicated check/Hero blocks across Fear, Void Warp, Frostbite, Enfeeble, Tempest Surge, Vitality Lash, Breathe Fire, Electric Arc, Caustic Blast and Gale Blast. Recipient iteration and specialized riders remain local. Recipient progression resets both Guidance fields (around8396/8424/8455/8943). Shared committed-cast provenance is in persistence around2713; public live validation for Guidance/Hero pause types around2682. Owner tests: `tests/test_spell_save_provenance.py`; independent tests: `tests/test_spell_save_provenance_review.py`.

Reproduced correctness changes: exact prepared Witch Fear source; canonical no-slot cantrip identities and exact spent ranked slots; Guidance offered before Tempest/Vitality and recipient saves; target-owned Guidance preserved into Hero rerolls and cleared before next recipient; Guidance pause provenance checked like Hero pause. Independent review found and verified four repair classes: forged arithmetic, forged Guidance cast source, forged cantrip slots and omitted multi-target Guidance. Invalid target ownership and unsupported profiles reject on load.

Validation: owner consolidated201/1.24s includes terminal Fear, save/load/resume, continuous casting/persistence, focus/prepared/spontaneous paths. Independent9/0.26s plus128/0.84s cross-adopter checks; review covers saved Guidance/Hero, distinct recipients, area continuation and Tempest clumsy. Owner/reviewer report clean final scoped process audits. Final canonical initially exposed a legitimate `spell_attack` Guidance profile omitted by new validation; retained owner restored it, focused14 passed, then justified canonical rerun1224/5.91s, compile/diff clean, peak82,149,376bytes. Root reactivated retained Sol solely to independently verify this final repair before whole-group acceptance. No new assignment boundary, code reassignment or duplicate broad review run.

Actual scheduled wake and useful retained-worker follow-up now verified through substantive final evidence; no acknowledgement-only or additional scheduler probe required.

## Accepted spell-save group and preparation dispatch — 23:53 UTC

Final independent repair verification: `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src .venv/bin/python -m pytest -q tests/test_s3_core.py::test_guidance_use_and_hero_reroll_preserve_bonus_and_saved_dice tests/test_spell_save_provenance_review.py tests/test_spell_save_provenance.py`: **14 passed/0.21s**. Confirms saved Guidance→Divine Lance spell attack→Hero keeps +1 and deterministic dice; adversarial provenance checks remain valid. No edits/broad rerun; final independent process filter found no matching retained processes. Rules anchors: [Divine Lance](https://2e.aonprd.com/Spells.aspx?ID=1498), [Guidance](https://2e.aonprd.com/Spells.aspx?ID=1549), [Hero Points](https://2e.aonprd.com/Rules.aspx?ID=2333), [basic saves](https://2e.aonprd.com/Rules.aspx?ID=2297), checked by reviewer and recorded in `docs/implementation/s1-s3-rules.md` around116–127 and review-test header. Root accepts complete group.

Closed usage: Terra/high15,156,006input /14,780,416cached /51,555output,113requests,1compaction. Sol/high9,005,769input /8,649,600cached /37,568output,73requests,1compaction. Combined24,161,775input /23,430,016cached /89,123output,186requests,2compactions. Cached input is a subset; dollar cost unavailable. One implementation owner, no replacement. Four consolidated reviewer repair classes and one canonical-discovered supported-profile repair retained same owner/reviewer. First baseline report arrived during initial dispatch turn; first refactored executable checkpoint86/0.71s received at23:28 wake. Acceptance23:53 UTC, approximately37minutes after initial23:16 dispatch; these are reported/wake times, not exact execution duration. No unresolved user-decision wait; review/final-verification waits occurred, exact durations unavailable.

The retained Terra owner now owns finite preparation consistency with new boundary `generalization-preparation-implementation-2026-09-18`; retained Sol exclusively reserved under new boundary `generalization-preparation-review-2026-09-18`. Both boundaries captured before new assignment/activation; earlier spell records closed. Complete routing/validation across Wizard/Druid/Witch/fixed preparations, publicly reproduce Warpriest lead, preserve early/late persistence stages, book/curriculum/menu/duplicate/source/slot facts. All-layer ownership, full-group review, direct ordinary repairs and final canonical apply. Any canonical-discovered production repair receives narrow independent verification before final acceptance. No baseline rerun needed.

## Preparation first executable checkpoint — September19 00:00 UTC

Owner publicly reproduced fixed-Warpriest daily preparation accepting an unsupported replacement: finish encounter, record rest, replace ordinary_heal_1 with force_barrage; command incorrectly returns COMPLETED. Regression fails as intended before repair. Finite shared choice/rejection dispatcher will cover daily preparation, casting snapshots, early/late saved validation and terminal, preserving Wizard-owned inventory/curriculum and exact fixed slots. Reserved Sol reviewer had a model-capacity service failure; same peer/model/accounting retained, owner instructed to retry activation when complete group runnable, report recurrence without polling or substitution. Implementation is not blocked.

## Accepted preparation group — September19 00:15 UTC

`src/pf2e/preparation.py` now supplies variable-policy detection, ordered slot choices and shared rejection dispatch to explicit Wizard/Druid/Witch policies with exact fixed fallback. Adopted in family_casting prepared snapshots (~673), Encounter.daily_prepare (~2634), committed prepared-cast validation, persistence early actor-free and later inventory-aware stages, terminal._choose_daily_preparations (~1074). Prior repeated family dispatch and terminal book-menu construction removed. Wizard owned-book/curriculum restrictions, source/rank/cantrip/spent/slot facts and duplicate/menu ordering retained.

Public reproduced fixed-Warpriest replacement now atomically rejects. Two consolidated reviewer repairs retained same owner: malformed/empty spell IDs reject before Witch membership rather than TypeError; Wizard curriculum validates cantrip identity with source/rank, preventing Breathe Fire/Force Barrage as free repeated cantrips while preserving ranked menus. No schema/content expansion. Owner coverage `tests/test_preparation_consistency.py`; independent `tests/test_preparation_consistency_review.py`.

Validation: owner168/1.31s including Druid duplicate terminal/victory-to-next-scene, Wizard inaccessible-book/substitution/curriculum, Witch victory→prepare→save/terminal next cast and fixed fallback. Independent7/0.28s, consolidated18/0.41s covers malformed atomicity, ordered menus, early/late save validation and saved next-scene casting. Final canonical1232/6.11s, compile/diff clean, peak82,739,200bytes, save17. Final owner/reviewer scoped process audits clear. Rules checked by reviewer: [Wizard](https://2e.aonprd.com/Classes.aspx?ID=39), [Battle Magic](https://2e.aonprd.com/ArcaneSchools.aspx?ID=22), [Druid](https://2e.aonprd.com/Classes.aspx?ID=34), [Witch](https://2e.aonprd.com/Classes.aspx?ID=38), existing fixed Warpriest ledger. Local anchors wizard_content131–134, druid_content26, witch_content37–38 and s1-s3-rules112.

Closed actual usage: Terra/high9,469,163input /9,344,256cached /19,703output,51requests,0compactions. Sol/high3,801,140input /3,724,416cached /17,387output,28requests,0compactions. Combined13,270,303input /13,068,672cached /37,090output,79requests,0compactions. Cached subset; dollars unavailable. One retained implementation owner, no changes; two ordinary review repairs. First reproduced failing public check and110-test runnable group reported at00:00 wake; accepted00:15, about22minutes after23:53 dispatch. These are report/wake timing, not precise execution duration. A Sol capacity failure caused review dependency delay, recovered once via same-peer followup; exact wait duration unavailable. No broad checkpoint repair/rerun.

Root dispatches next coherent group, admitted bomb-fact consumption, under new reused boundaries `generalization-bomb-facts-implementation-2026-09-18` and `generalization-bomb-facts-review-2026-09-18`. Retains same owner/reviewer, records boundaries before activation. Astra read-only `generalization-recipient-assessment-2026-09-18` independently assesses residual recipient duplication after settled save seam to recommend final scope versus diminishing returns. No competing production edits.

## Final-scope decision — September19 00:23 UTC

Astra read-only residual-recipient assessment recommends stopping after accepted bombs; root adopts that recommendation. No edits/probes/tests or retained processes, and no material new inconsistency established. Current high-risk Guidance/modifiers/Hero/resume is already shared. Remaining duplicated index parsing, recipient exhaustion/advance, Guidance reset and basic-save damage reuse occur in Breathe Fire resolve/continue(~8335/8388), Electric Arc(~8503/8935), Caustic continuation(~8416), Gale(~8447), post-damage branches(~6153–6202).

Semantics differ materially: Breathe Fire snapshots cone targets and finishes ranked/Arcane Bond casting; Electric Arc rechecks live eligibility for one/two selected targets and emits dissipated-arc events; Caustic preserves critical-failure flags into persistent acid after damage; Gale preserves push distance, damage-before-push, self/emanation and delayed encounter completion across recipients. A complete shared iterator would need eligibility/skipping/completion/post-damage policies, mostly moving the same branches; a small parser/reset helper saves few lines without simplifying lifecycle. No callbacks or saved-state schema are warranted.

Independent existing protection: spell_save_provenance_review tests prepared_multi_recipient_save_resumes_shared_roll_and_second_target, multi_recipient_save_offers_each_targets_distinct_guidance, area_save_hero_pause_round_trips_current_recipient; wizard_direct_spells electric_arc_saves_before_each_recipient_resolves_once; movement_spells_review saved_gale_health_choice_commits_once_then_drops_before_push and gale_finishes_only_after_every_snapshotted_recipient. These were inspected, not rerun by assessment.

Deferred opportunity: revisit Breathe Fire/Electric Arc lifecycle when another admitted spell actually shares it or a progression defect establishes drift; preserve stage strings/ActionContinuation fields and retain Caustic/Gale riders. Not a content-expansion prerequisite. Paired-policy and turn-walk cleanup remain optional. One-poison, generic-minion and universal-effect frameworks remain unjustified. Complete bomb group review/integration then pause this phase heartbeat and report diminishing returns.

## Accepted bomb group and phase completion — September19 00:36 UTC

`alchemist_content.admitted_bomber_bomb_facts` (~12–22) admits only BOMBER_FIELD_FORMULA_IDS; selected AttackDefinitions derive damage/range from facts (~25–45). Encounter uses selected AttackDefinition.item_id for options/Quick Bomber (~3595/4402), hit/miss/area splash and riders/deadlines (~5023/5064/6237/6302–6411). Terminal derives formula menus from BOMBER_FORMULA_IDS/names and selected attack range (~2239–2312). Independent static audit confirms replaced two-ID switches and hard-coded splash/type numerics removed from Encounter/terminal; only finite supported rider/deadline translation remains. Persisted attack_id rederives immutable facts from actor definition; no persistence/schema change.

Two publicly reproduced correctness fixes: terminal Quick Bomber previously listed stowed bombs but used ordinary held-Strike targets, yielding no targets; now selected bomb range provides valid target and terminal probe plays throw. Critical-failure bombs wrongly applied primary splash (Bottled Lightning, supplied rolls20/1/1, dog8→7); now only ordinary failure splashes, critical failure still consumes. Sol found and independently verified latter source-backed repair. Preserve combined primary defenses, undoubled critical splash, saved per-throw primary-only scope, distinct rider anchors, infusion creator/expiry/one-use provenance, two-bomb admission and unsupported failure-defense pause.

Owner relevant70/0.45s; independent8/0.22s (`tests/test_bomb_facts_review.py`), consolidated20/0.31s cover admission/reference-only rejection, fact-derived attack, miss/critical/defenses/area/deadline, saved Hero/consumption/tamper cases, Quick Alchemy provenance, terminal and day-two next-scene stock. Owner source/linkage and terminal tests in `tests/test_bomber_alchemist.py` around22–48/332–350. Final canonical `PYTHONPATH=src .venv/bin/python tools/integration_checkpoint.py`: **1242/5.99s**, runner6.298s, peak83,820,544bytes, compile/diff clean, save17. No final canonical repair required. Owner and independent reviewer scoped process inventories clear; no commits.

Reviewer sources: [Quick Bomber](https://2e.aonprd.com/Feats.aspx?ID=5764), [bomb rules](https://2e.aonprd.com/Rules.aspx?ID=3181), [Bomber](https://2e.aonprd.com/ResearchFields.aspx?ID=5), [Bottled Lightning](https://2e.aonprd.com/Equipment.aspx?ID=3290), [Frost Vial](https://2e.aonprd.com/Equipment.aspx?ID=3293), [Quick Alchemy](https://2e.aonprd.com/Actions.aspx?ID=2801); local content matrix `docs/implementation/pc1-pc2-encounter-content.md:11`.

Closed bomb usage: Terra/high7,686,167input /7,486,720cached /26,832output,59requests,1compaction; Sol/high4,669,752input /4,608,512cached /13,679output,24requests,0compactions. Cached subset; dollars unavailable. One retained owner and reviewer; one source-backed review repair; final canonical passed first attempt. First coherent checkpoint53/0.45s and source repair53/0.44s reported by00:29 wake; accepted00:36, approximately20minutes from00:15 dispatch. Timing is report/wake-based, exact execution/wait durations unavailable.

Final read-only Astra assessment usage:354,151input /336,000cached /1,615output,4requests,0compactions, actual Astra/high; no code/tests/processes.

The authorized implementation phase is complete: three accepted groups, no speculative architecture or content expansion. Original phase baseline1211/5.98s/81,952,768bytes versus final1242/5.99s/83,820,544bytes; measurements are canonical suite timing/RSS, not a fresh command/menu latency benchmark. Accepted L1 remains14/16, Bard/Monk partial and L2 unaccepted; catalog/admission and save17 boundaries preserved. Final-scope rationale above explains diminishing returns. New execution heartbeat is to be paused through the app tool; previous content coordinator, workers and automation remain paused. Remaining work is documentation/accounting closure only.

## Final closure — September19 00:40 UTC

AGENTS/README/STATUS completion update finished, touched links and diff whitespace passed; root plans/recovery/evidence updated. App automation tool returned pf2e-generalization-execution **PAUSED**, preserving its other fields. Prior content/audit automation unchanged. Root final scoped process audit at00:39:19UTC found only its audit commands, no retained matching engine tests/probes; no termination required.

Completion-guidance actual Terra/high usage288,365input /244,480cached /3,779output,5requests,0compactions. Existing deterministic milestone CLI across all ten phase assignment IDs returned **51,349,177input /49,991,168cached /180,975output,374requests,3compactions,10runs**. All records closed. These totals cover workers, not coordinator; cached input is a subset and dollar cost unavailable. Approximately84minutes from initial23:16 dispatch to00:40 closure includes scheduler/review/docs time. No owner replacement across three implementation groups. No active worker dependency or refactoring decision remains.
