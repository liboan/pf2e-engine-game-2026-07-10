# Completed coordination history: caster admission and Justice

This historical record was moved out of the compact [current-work page](../ACTIVE.md). It is not an active task queue. All entries below are closed.

## Worker reporting

During Sure Strike delivery, an app event marker lagged behind active work. A delegated metadata check established continued progress. A bounded report-recovery pass found the owner's progress messages at 13:06:58, 13:07:23 and 13:07:50 UTC, followed by its internal final handoff. No interruption, owner change or process termination was warranted. These diagnostics did not inspect or verify implementation.

A later Justice metadata diagnosis likewise found fresh current-turn requests and no reported blocker. Worker-local commentary was not automatically relayed to root. Compact app status summaries later recovered concrete test results; final native reports supplied the acceptance evidence. This was a reporting limitation, not proof of stalled implementation. The diagnosis cost 384,552 input tokens (378,880 cached), 2,316 output over four requests. The separate documentation-only roster handoff cost 216,647 input (193,280 cached), 4,803 output over five requests.

## Investigator handoff accounting correction

Root mistakenly registered the reused execution-handoff assignment twenty seconds after sending its prompt. The initial cursor excluded two early requests. Attempts to repair only visible cumulative values and timestamps were insufficient because the collector selects records with its request cursor.

A delegated collector diagnosis supplied the exact preceding five-request cursor from the previous completed roster assignment. Root applied it to its own ledger and recomputed through the existing collector. No engine or collector code changed. The ledger preserves the original registration time and correction reason.

Correct `investigator-execution-handoff-2026-09-16` totals: **5,157,662 input / 4,936,192 cached / 14,273 output**, 34 requests including one compaction. The administrative diagnosis cost **1,650,677 input / 1,604,096 cached / 14,856 output**, 14 requests. Cached input is a subset of input. Dollar costs are unavailable.

Prevention: register reused assignments before dispatching the prompt; retain one run across checkpoints, waits and ordinary review repairs; close once at acceptance. Recomputing a closed reused assignment requires its original cursor and must never substitute cumulative worker totals for assignment usage.

## Earlier process audit

The post-caster scoped inventory found no attributable project pytest/game/Chrome/Chromium leftovers. Python PID 23701 was an unrelated localclaw benchmark, confirmed by working directory. Node entries were shared ChatGPT.app MCP services or another project. No process was terminated. That audit cost 276,161 input tokens (268,544 cached), 1,570 output over three requests. Later process audits belong in the current-work page and ledger.
